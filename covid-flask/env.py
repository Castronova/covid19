#!/usr/bin/env python3

from os.path import dirname, join

template_dir = join(dirname(__file__), 'templates')
static_dir = join(dirname(__file__), 'static')
data_path = join(dirname(__file__), 'static/dat')




